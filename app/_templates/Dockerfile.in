FROM ${IMAGE_FROM}

MAINTAINER ${IMAGE_MAINTAINER}

ENV \
    APP_HOSTNAME="${APP_HOSTNAME}" \
    COMPOSER="${COMPOSER}" \
    GIT_REF="${GIT_REF}" \
    WP_EXTRA_CONFIG="${WP_EXTRA_CONFIG}" \
    WP_TITLE="${WP_TITLE}"

COPY . /app/
